module.exports = {
    init: initialize,
    fixtures: fixtures,
    close: close
}

function initialize(callback) {
  callback();
}

function fixtures(data, callback) {
  callback();
}

function close(callback) {
  callback();
}
